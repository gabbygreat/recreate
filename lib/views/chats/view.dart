part of 'controller.dart';

class ChatsView extends StatelessView<ChatsScreen, ChatsController> {
  const ChatsView(super.state, {super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: ComingSoon(),
    );
  }
}
