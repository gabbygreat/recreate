part of 'controller.dart';

class ExerciseView extends StatelessView<ExerciseScreen, ExerciseController> {
  const ExerciseView(super.state, {super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: ComingSoon(),
    );
  }
}
